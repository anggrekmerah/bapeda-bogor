extends ../layout

block content
    h1(class="mt-4") #{title}
    div(class="container-fluid border p-2" style="margin-top : 35px;")
        
        //- if btnadd
        //-     a(class="btn btn-primary float-end mb-2" href="group/add") Add Group

        table(class="table table-sm table-bordered display compact table-striped" id="tableReportKpi")
            thead 
                tr
                    th(scope="col" class="text-center") No
                    //- th(scope="col") Group 
                    //- th(scope="col") Group Desc
                    //- th(scope="col") Created By 
                    //- th(scope="col") Updated By 
                    th(scope="col" class="text-center") Action
            tbody 
               
